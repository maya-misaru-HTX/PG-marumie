# Google Cloud Vision API Configuration
# This app uses Google Cloud Vision API for PDF OCR processing
# Supports files up to 20MB

# IMPORTANT: Fallback behavior without credentials:
# - Uses OCR.space free tier (1MB limit)
#
# For large files or high-accuracy OCR, Google Cloud Vision API is recommended

# Option 1: Local development - Path to service account key file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json

# Option 2: Vercel/Production - Service account key JSON content (minified to single line)
GOOGLE_CREDENTIALS={"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}

# Google Cloud Storage bucket name (optional)
# If not specified, {project_id}-ocr-temp will be used automatically
GCS_BUCKET_NAME=your-bucket-name

# OCR.space API Key (fallback, optional)
# Used when Google Cloud credentials are not available (1MB limit)
OCR_SPACE_API_KEY=K87899142388957

# Setup instructions:
# 1. Refer to GOOGLE_CLOUD_SETUP.md to create a Google Cloud Project
# 2. Enable Cloud Vision API
# 3. Create a service account and download the JSON key
# 4. Copy this file to .env.local and set your actual values
